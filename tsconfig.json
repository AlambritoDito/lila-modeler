{
  // `apps/web` queda fuera de este build a propósito: es una app de Vite con
  // `noEmit`, incompatible con los proyectos `composite` que usa `tsc --build`.
  // Se comprueba con `npm run typecheck` (llama a `typecheck` de @lila/web).
  "files": [],
  "references": [{ "path": "packages/engine" }, { "path": "packages/mcp" }]
}
