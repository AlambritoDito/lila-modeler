# Beta 1 verification report — `v1.0.0-beta.1`

Reproducible record of how the first public beta was validated. Historical: it describes the
candidate named below, not the current state of `main`.

| Item | Value |
| --- | --- |
| Candidate (`main` at tag time) | `<RC-SHA>` |
| Tag | `v1.0.0-beta.1` (annotated) |
| Base of the work | `09c205b9cc2f4f4122e957cf7ec02a0a72277899` (2026-09-22 audit candidate) |
| Host | macOS 27.0 (26A428), arm64 |
| Node / npm (local) | 24.16.0 / 11.13.0 — CI: Node 22 and 24 (ubuntu-latest) |
| Browsers | Chrome <CHROME-VERSION>; Safari <SAFARI-VERSION> via safaridriver |
| Packaged runtime | Electron <ELECTRON-VERSION> |

## Pull requests merged into the candidate

| PR | Issue | Scope | QA |
| --- | --- | --- | --- |
| <PR-356> | #356 | conditional replication statistics, `n`, warning, contracts | `QA: OK` |
| <PR-378> | #378 | `.lila` opened through launch/open-file saves | `QA: OK` |
| #383 | #381/#380 | landing, colleague guide, Mac beta guides | `QA: OK` |
| #382 | #381 | release/desktop/pages workflow gates | `QA: OK` |
| <PR-ADAPTERS> | #356 | results/compare views: legacy-result notice | `QA: OK` |
| <PR-VERSION> | #381 | version `1.0.0-beta.1`, changelog, this report | `QA: OK` |

## Source gates on `<RC-SHA>` (fresh clone, `npm ci`)

| Check | Command | Result |
| --- | --- | --- |
| Install | `npm ci` | <RESULT> |
| Build | `npm run build` | <RESULT> |
| Full suite | `npm test -- --maxWorkers=2` | <RESULT> |
| Root typecheck | `npm run typecheck > /dev/null 2>&1; echo EXIT=$?` | `EXIT=<N>` |
| Desktop typecheck | `npm run typecheck -w @lila/desktop` | <RESULT> |
| Links | `npm run check:links` | <RESULT> |
| Package | `npm run test:package` | <RESULT> |
| Pages build | `npm run build:pages` | <RESULT> |
| Root-base web build | `npm run build -w @lila/web` | <RESULT> |
| Project E2E | `LILA_E2E_PORT=18571 LILA_E2E_CDP_PORT=19571 node tools/e2e-lila.mjs` | <RESULT> |
| Replay E2E | `LILA_E2E_PORT=18572 LILA_E2E_CDP_PORT=19572 node tools/e2e-replay.mjs` | <RESULT> |
| Desktop open-path E2E | `node tools/e2e-desktop-open-path.mjs` | <RESULT> |
| CI (merged SHA) | Node 22 / Node 24 | <RUN-URL> |

## Web acceptance (`/lila-modeler/app/` served from the Pages artifact)

Same check set in both browsers: startup/assets, edit, AS-IS and TO-BE (seed 42, 30 replications),
comparison, replay, four CSV exports and XLSX, explicit `.lila` download, reload restore, discard,
reopen after clearing storage, invalid file, cancelled open, storage quota failure, cancelled run,
failed editor bundle recovery, five themes × English/Spanish with persistence, no page errors.

| Browser | Version | Result | Evidence |
| --- | --- | --- | --- |
| Chrome | <CHROME-VERSION> | <RESULT> | `evidence/web-chrome/report.json` |
| Safari | <SAFARI-VERSION> | <RESULT> | `evidence/web-safari/report.json` |

## macOS arm64 acceptance

| Check | Result |
| --- | --- |
| Local DMG from `<RC-SHA>` (`npm run dist:mac`) | `Lila Modeler-1.0.0-beta.1-mac-arm64.dmg`, SHA-256 `<LOCAL-DMG-SHA256>` |
| CI DMG attached to the release | SHA-256 `<CI-DMG-SHA256>` (matches `SHA256SUMS`) |
| Architecture / bundle version | `lipo -archs` = arm64; `CFBundleShortVersionString` = 1.0.0-beta.1 |
| Packed web bundle | five theme JSONs identical to source; startup shows `v1.0.0-beta.1` |
| Menu open → edit → Save → close Save/Discard/Cancel → reopen | <RESULT> |
| Launch argument / `open-file` / recents / dialog → Save, Save As, close | <RESULT> |
| Finder double-click `.lila` and `.bpmn`, app closed and running | <RESULT> |
| Quarantined download: mount, copy, `spctl --assess`, first launch | <RESULT> |

## Verdicts

- **Web**: <VERDICT> — <LIMITATIONS>
- **macOS arm64**: <VERDICT> — unsigned, not notarized; <LIMITATIONS>

Untested and therefore unsupported: Intel Macs, Windows, Linux, macOS versions other than 27.0,
browsers other than the versions above, mobile editing.
