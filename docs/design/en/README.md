# English application screenshots

Captured from the built application on 2026-09-09 using the default browser viewport
(1280 × 720), Eva-01 theme, English interface and the restaurant example with English labels.

- `model.png`: initial BPMN modeling view.
- `simulate.png`: AS-IS scenario editor.
- `results.png`: AS-IS results, seed 42 and 30 replications.
- `compare.png`: AS-IS versus TO-BE 3 cashiers, after saving and reloading the project.

These are application screenshots, not design mockups. Both scenarios were run in the browser.
The comparison keeps long per-run warning lists in expandable sections; no warnings are removed.
The Pages build copies these files into the landing page's `img/` directory.
